pipeline {
    agent any
    
    stages {
        
        stage('Permissions') {
           echo 'permissions'
        }
        
        stage('Cleanup') {
            echo 'cleanup'
        }
        
    }
}